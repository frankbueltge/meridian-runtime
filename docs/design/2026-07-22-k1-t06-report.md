# K1-T06 packet report — blind primary-source verification of the two real claims

**Task identifier:** task-packets/K1-T06.yaml (candidate c of
`docs/design/2026-07-22-verifikations-entscheidung-und-bauprogramm.md`, Entscheidung 1).
Executed 2026-07-22, immediately after PR #60 (K1-T05) merged.

## Implementation summary

A blind verifying instance — fresh model instance, isolated context, no tool/repository/
database/web access, tools named generically per the house rule — received only: the
research question, the charter criterion `instantiate-vs-reference-v1` verbatim, the 15
cluster-7 works' raw `decisive_move` texts from the pinned works-atlas snapshot
(alphabetical order, no classification data), and the 3 theory papers' abstracts
**re-fetched from arXiv on 2026-07-22** (stop-condition honored: never from memory or
atlas summaries). Its verdicts were frozen by commit BEFORE any comparison (AT1):

- `dab655c` — blind brief frozen (step 1)
- `f4136f1` — blind returns frozen verbatim (step 2)
- comparison + authored VerificationResults (step 3), CLI outputs (step 4) follow in
  this branch's later commits.

**Result: 18/18 row agreement, zero disagreements, zero `qualifies`.**
14× references-only + 1× instantiates (Hammond V3) reproduces the pipeline's
classification exactly; 3× supports reproduces the theory relations. The blind instance
independently drew the same boundary lines the charter prescribes (Giraud: human
generational relay; Baudelaire: conversation loop ≠ training loop; Cheng: inference-time
belief updating; Rahal: environmental audio feedback; Kurant: the artist's own writings
are human-authored input — the criterion's named case).

## Recorded verifications (AT2)

Recorded via `mrr verification record` (K1-T05) against schema
`mrr_k1t04_real_run_v2`, run-executor `urn:mrr:node:01KY1SNYATEVDJGYNGFRZ6S18R`,
policy `policy-mrr-k1-t06-blind-verification`; both exit 0; both
`verification.recorded` domain events present (2/2, atomicity confirmed by direct
schema inspection):

| Claim | VerificationResult | Recommendation | Confidence | Claim status after |
|---|---|---|---|---|
| `urn:mrr:claim:01KY1SNYKA19APBDVN1GQ3RQFS` (instantiation-vs-reference, contested) | `urn:mrr:verification:01KY4PZ13XBPPWVNB3H30MD02K` | pass | 0.85 | contested (unchanged — honest) |
| `urn:mrr:claim:01KY1SNYN45EM4Z1VSS3S97Z2M` (theory-confirmation, draft) | `urn:mrr:verification:01KY4PZ1415344RA8QCAW5YG1X` | pass | 0.95 | draft (unchanged — honest) |

Reviewer identity: `urn:mrr:agent-role:01KY4PZ13XBPPWVNB3H30MD02H` (differs from
proposer `…86X0…` and executor node — rule 8 satisfied on substance: distinct instance,
distinct prompt lineage, no pipeline execution; AT4). A pass recommendation does not
auto-promote a claim's status — any promotion (e.g. the theory claim's draft →
supported, now that a verification exists) is a separate governance act, deliberately
NOT taken inside this packet.

**Finding (minor), on the instantiation claim:** the übermorgen row's underlying source
verification remains `pending` in the pinned corpus; blind classification agreement does
not substitute for it (derived_decision (c)).

## R5 resolved as FALSE — scope note

The run-1 and run-2 schemas hold **id-distinct** claim objects (no cross-schema id
stability; K1-T04b's invariance proof was within-run). Per R5's own conditional, the
binding scope is the run-1 claims (`mrr_k1t04_real_run_v2`) — the K2 gate's subject.
The run-2 claims (schema `mrr_run2_corroboration_floor_v1`) are substantively covered
by the same blind re-reading (same corpus, same criterion); mirroring the recording
there is a one-command act left for a future session if wanted, not silently assumed.

## Commands executed

- `mrr verification record` ×2 — exit 0 both (outputs committed:
  `corpora/model-collapse/verification/cli-output-*.json`)
- `make test-integration` — **205 passed** (suite unchanged; no code touched)

## Files changed

Only `corpora/model-collapse/verification/` (new: blind-brief.md, blind-instance-note.md,
blind-returns.json, comparison.md, verification-*.json, cli-output-*.json) and this
report. No code, no schemas, no migrations, no sealed artifact touched
(forbidden_changes honored).

## Security / privacy implications

None: no new code paths, no secrets, no external calls beyond two arXiv abstract
fetches over HTTPS; the blind instance had no credentials.

## Known limitations

1. Independence is substantive, not institutional — same responsible human, same model
   family as the tooling; declared verbatim in each VerificationResult's
   conflicts_of_interest and independence_profile (R4).
2. The blind pass judges the pinned `decisive_move` texts against the criterion; it does
   not re-verify the atlas's own source research (that is the works-atlas's own
   verify_status layer, and — for the one pending row — an open item on the record).
3. Candidate d (Ulysses, work-atlas claim, bounded Hammond review) is a separate voice,
   offered 2026-07-22 through `irrtum-als-methode/REQUESTS.md`; if taken, it records as
   its own VerificationResult (derived_decision (d)) — not folded into this one.

## Specification conflicts discovered

None. The contract's recommendation vocabulary (`pass|fail|inconclusive`) sufficed;
the specification_gaps note ("agrees with reservations") was not needed — reservations
fit honestly in findings + rationale.

## K2 gate consequence

With this packet, the K2 gate's trigger 2 is now **half-satisfied twice over**:
MTH-018 execution (K1-T03b) ✓ and independent verification of the two real claims ✓
(this packet). The gate's re-evaluation remains a deliberate act for a future session
per the 2026-07-22 decision record — after E8/E9, or earlier if the owner calls it.
