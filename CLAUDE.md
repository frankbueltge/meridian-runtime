# Claude Code entrypoint

Read `AGENTS.md` first. Then read only the specification sections referenced by the active task packet.

Do not attempt to implement the entire system in one pass. Work in bounded pull-request-sized changes and return evidence that all specified acceptance commands pass.
