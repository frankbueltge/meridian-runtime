{
  "entries": [
    {
      "entry_id": "model-collapse-mechanism-v1",
      "term": "model-collapse mechanism",
      "definition": "The technical-literature process by which a generative model trained iteratively on its own (or its predecessors') outputs suffers a measurable degeneration of its output distribution — narrowed tail coverage, amplified mode bias — across successive training generations.",
      "scope_note": "Local operationalization for this profile only; not a claim about every use of the phrase in the wider literature. Applied here (task-packets/K1-T04.yaml) to the three theory-atlas papers matching a fixed, disclosed keyword search (\"model collapse\", \"model-collapse\", \"self-consuming\", \"self-consumption\", \"autophagy\", \"MAD\", \"curse of recursion\") over tags/summary/work: shumailov-curse-of-recursion, alemohammad-self-consuming-generative-models-go-mad, gerstgrasser-is-model-collapse-inevitable."
    },
    {
      "entry_id": "instantiate-vs-reference-v1",
      "term": "instantiate",
      "definition": "A work 'instantiates' the mechanism when its own production pipeline demonstrably trains on model-generated outputs across at least two generations; a work merely 'references' the mechanism when it only discusses, depicts, or alludes to it without demonstrable recursive training. Decision procedure, applied directly against each work's own `decisive_move` + `source_url` field in the pinned works-atlas snapshot: Step 1 -- does the work's own documented production process feed model-GENERATED output back in as training input for a SUBSEQUENT generation of the same (or a continuous) generative pipeline, across at least two generations? If yes -> instantiates. Step 2 -- if the work only depicts, critiques, or thematizes the phenomenon, or trains on human-authored material (even the artist's own prior writing) without a demonstrable model-generation-to-model-generation loop -> references only. Ambiguous or incompletely disclosed cases -> qualifies, assessed individually, never force-fit into either bucket.",
      "scope_note": "Applied here (task-packets/K1-T04.yaml) to the 15 works-atlas entries whose own `clusters` array contains 7 (\"AI self-consumption\"); each classification's basis is recorded in corpora/model-collapse/corpus-entries.json's own `extraction.classification_basis` field, quoting the exact `decisive_move` text it rests on."
    },
    {
      "entry_id": "corroboration-floor-v1",
      "term": "independent corroboration",
      "definition": "This entry names a SENSITIVITY VARIATION on the eligibility threshold this profile's own `eligibility_rules` already uses — specifically, how many distinct, independent source families must corroborate an analysis's supporting or contradicting side before that side counts as adequately corroborated. The BASE run's own protocol-parameters sidecar sets `contested.min_independent_source_families = 1` (an analysis may be called 'contested' if even a single independent family supports it, so long as at least one independent family also contradicts it) alongside `supported.min_independent_source_families = 2` (an analysis may only be called 'supported' with at least two independent corroborating families) — an asymmetry between the two labels' own evidentiary bar. `corroboration-floor-v1` RAISES the 'contested' floor to match the 'supported' floor exactly — `min_independent_source_families = 2` for BOTH outcome labels — asking whether this profile's own classifications still hold when a single independent supporting family is no longer sufficient to call an analysis 'contested' either, requiring the SAME minimum corroboration strength regardless of which side of a finding is being assessed. This is explicitly a THRESHOLD VARIATION over the existing `eligibility_rules` mechanism (MRR-MTH-018) — it changes how much independent corroboration is required before a classification is reported, not what 'instantiate' or 'reference' or 'model-collapse mechanism' MEANS. It is NOT a second, rival operationalization of the mechanism-vs-reference distinction (contrast entry `instantiate-vs-reference-v1`, whose own definition this entry leaves entirely untouched) and MUST NOT be read as one.",
      "scope_note": "Applied here (task-packets/K1-T04b.yaml) as this run's own sole declared `MethodProtocol.sensitivity_variations` entry, executed via the `SensitivityVariationParameters` sidecar `corpora/model-collapse/run2-corroboration-floor/sensitivity-variation-parameters.corroboration-floor-v1.sidecar.json` over the SAME corpus and base protocol-parameters run 1's own base classification already used (`corpora/model-collapse/corpus-entries.json`, `corpora/model-collapse/protocol-parameters.sidecar.json`, both reused unchanged by direct path reference, never copied or re-extracted, per derived_decisions (b))."
    }
  ]
}
