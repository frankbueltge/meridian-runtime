"""Systems evaluated by MeridianBench: the scripted agent-under-test
(``scripted_agent``, a fake ``mrr.domain.model_adapter.ModelAdapter``) and
the non-agent baseline (``baselines``). See each module's own docstring.
"""

from __future__ import annotations
