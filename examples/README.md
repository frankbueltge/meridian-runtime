# Examples

These examples are schema-oriented fixtures for implementation bootstrapping. Identifiers, hashes, signatures, and timestamps are illustrative. Integration tests must generate cryptographically valid values with the implementation's canonicalization and signing functions.
